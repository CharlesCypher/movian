import { useParams } from "react-router-dom";

const MovieDetails = () => {
  const { movieId } = useParams();
  return (
    <>
      <main>
        <div className=""></div>
      </main>
    </>
  );
};

export default MovieDetails;
